/**
 * 
 testing github integration
 Creep Button
 *
 */

var UI = require('ui');
var Vibrate = require('ui/vibe');
var Vector = require('vector2');
var Accel = require('ui/accel');

Accel.on('tap', function(e) {
  // something goes here
});

Accel.init();
